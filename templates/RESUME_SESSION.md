# Resume Session - Quick Top-of-Context

> Top-of-context **handoff** for a fresh / just-compacted session - a **pointer, not a second reference
> doc**. Auto-injected by the `agent-tiers` SessionStart hook. Keep it light + current (update as you
> work, not batched). Untracked + machine-specific.

**HEAD:** `{{commit}}` ({{version/state}}). Branch: `{{branch}}`. Tree: {{clean/dirty}}.
Deployed/built: {{state}} - {{verified / UNVERIFIED}}.

## ▶ NEXT
1. {{the exact next step}}

## Context this chunk
- Goal: {{what we're doing}}
- Changed: {{files}}
- Decisions: {{key calls}}
- Failed / ruled out: {{so we don't repeat it}}

## Build / test
- {{build cmd}} · {{test cmd}} · {{verify note}}
