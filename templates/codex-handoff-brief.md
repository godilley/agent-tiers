# Codex handoff brief -> use the shared escalation-brief template

> v3 merged this into ONE template for all metered escalations (Advisor / Boss / Codex):
> `~/.claude/agent-tiers/templates/escalation-brief.md`
>
> For a Codex handoff fill: Goal · The ask (implementation-shaped) · Scope (touch only) ·
> Acceptance criteria · Constraints · Out of scope · Notes Codex can't infer. Feed it on stdin
> (`codex exec - < brief.md`) or via the `codex-write` agent (isolated worktree). This pointer file
> is kept so older references keep resolving; do not add content here.
